properties:
  slug:
    metadata:
      description: A unique identifier for the club
    type: string

  name:
    metadata:
      description: The name of the club
    type: string

  staffSupervisor:
    metadata:
      description: The supervisors of the club
    type: string

  clubLeaders:
    metadata:
      description: The leaders of the club
    type: string

  shortDescription:
    metadata:
      description: A short description of the club
    type: string

  extraInformation:
    metadata:
      description: Extra information about the club
    type: string

  categories:
    metadata:
      description: A comma separated list of categories the club belongs to
    type: string

  meetingTimes:
    metadata:
      description: The meeting times of the club
    type: string

  joinInstructions:
    metadata:
      description: Instructions about how to join the club
    type: string

  onlinePlatforms:
    metadata:
      description: Online platforms to find more information about the club (e.g. Instagram, Discord, etc.)
    type: string

  timeCommitment:
    metadata:
      description: The time commitment members need to participate in the club
    type: string

  equityStatement:
    metadata:
      description: The club's equity statement
    type: string
