{
  "name": "clubs-website",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev:frontend": "yarn workspace frontend dev",
    "prune:frontend": "rm -rf google-sheets-auto-pr",
    "build:frontend": "yarn workspace frontend build",
    "heroku-prebuild": "rm -rf frontend scripts"
  },
  "workspaces": [
    "frontend",
    "google-sheets-auto-update/backend",
    "scripts"
  ],
  "devDependencies": {
    "prettier": "^2.3.0"
  }
}
