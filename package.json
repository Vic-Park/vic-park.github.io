{
  "name": "clubs-website",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev:frontend": "yarn workspace frontend dev",
    "prune:frontend": "rm -rf google-sheets-auto-pr",
    "build:frontend": "yarn workspace frontend build",
    "heroku:google-sheets-auto-update": "yarn workspace google-sheet-auto-update-backend start:transpile-only"
  },
  "workspaces": [
    "frontend",
    "google-sheet-auto-update-backend",
    "scripts"
  ],
  "devDependencies": {
    "prettier": "^2.3.0"
  }
}
